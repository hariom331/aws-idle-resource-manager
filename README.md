# aws-idle-resource-manager
Serverless FinOps automation that detects idle EC2, Aurora and RDS resources, warns owners by email, and stops them if nobody responds. Terraform-managed, deployed through GitHub Actions with OIDC — zero static credentials.
